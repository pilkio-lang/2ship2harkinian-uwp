name: Build sm64ex NTSC-FR UWP

on:
  push:
    branches:
      - chatgpt-sm64ex-fr-build

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout build runner branch
        uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            base-devel
            git
            make
            python
            mingw-w64-x86_64-gcc

      - name: Record MSYS2 root
        shell: msys2 {0}
        run: |
          root="$(cygpath -w / | tr '\\' '/')"
          echo "SM64EX_MSYS2_ROOT=$root" >> "$GITHUB_ENV"

      - name: Fetch exact sm64ex UWP source and apply NTSC-FR patch
        shell: pwsh
        run: |
          git clone https://github.com/caorthann-celt/sm64ex-uwp.git sm64ex-src
          Set-Location sm64ex-src
          git checkout a0b33a006b0204d8fc837fa760a19135148c96bc
          git apply "$env:GITHUB_WORKSPACE\ci\sm64ex-ntsc-fr.patch"
          git apply "$env:GITHUB_WORKSPACE\ci\sm64ex-ntsc-fr-part2.patch"
          git diff --check

      - name: Build text converter
        shell: msys2 {0}
        working-directory: sm64ex-src
        run: |
          make -C tools textconv
          test -f tools/textconv.exe || cp tools/textconv tools/textconv.exe

      - name: Install SDL2 UWP
        shell: pwsh
        run: |
          if (-not $env:VCPKG_INSTALLATION_ROOT) {
            $env:VCPKG_INSTALLATION_ROOT = "C:\vcpkg"
          }
          $env:VCPKG_ROOT = $env:VCPKG_INSTALLATION_ROOT
          & "$env:VCPKG_ROOT\vcpkg.exe" install sdl2:x64-uwp
          "VCPKG_ROOT=$env:VCPKG_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build UWP game DLL and package
        shell: cmd
        run: |
          call "%ProgramFiles%\Microsoft Visual Studio\2022\Enterprise\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 -app_platform=UWP
          set "VCPKG_ROOT=C:\vcpkg"
          cd /d "%GITHUB_WORKSPACE%\sm64ex-src\ports\uwp"
          cmake --preset uwp-game-dll-release -DSM64EX_MSYS2_ROOT="%SM64EX_MSYS2_ROOT%" -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" -DSM64EX_VCPKG_ROOT="C:/vcpkg" -DSM64EX_SDL2_ROOT="C:/vcpkg/installed/x64-uwp"
          cmake --build --preset uwp-game-dll-release
          cmake --preset uwp-package-release
          cmake --build --preset uwp-package-release

      - name: Sign package as CN=Caorthann
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $out = Join-Path $env:GITHUB_WORKSPACE "deliverable"
          New-Item -ItemType Directory -Force $out | Out-Null

          $msix = Get-ChildItem "$env:GITHUB_WORKSPACE\sm64ex-src\ports\uwp\build-package" -Recurse -File |
            Where-Object { $_.Extension -in ".msix", ".appx" } |
            Sort-Object LastWriteTime -Descending |
            Select-Object -First 1
          if (-not $msix) { throw "No MSIX/AppX package was produced." }

          $cert = New-SelfSignedCertificate -Type Custom -Subject "CN=Caorthann" -KeyUsage DigitalSignature -FriendlyName "sm64ex NTSC-FR UWP" -CertStoreLocation "Cert:\CurrentUser\My" -TextExtension @("2.5.29.37={text}1.3.6.1.5.5.7.3.3","2.5.29.19={text}")
          $pwdText = "sm64ex-fr-build"
          $pwd = ConvertTo-SecureString -String $pwdText -Force -AsPlainText
          $pfx = Join-Path $out "sm64ex-NTSC-FR-signing.pfx"
          $cer = Join-Path $out "sm64ex-NTSC-FR.cer"
          Export-PfxCertificate -Cert $cert -FilePath $pfx -Password $pwd | Out-Null
          Export-Certificate -Cert $cert -FilePath $cer | Out-Null

          $signtool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin" -Recurse -Filter signtool.exe |
            Where-Object { $_.FullName -match "\\x64\\signtool\.exe$" } |
            Sort-Object FullName -Descending |
            Select-Object -First 1
          if (-not $signtool) { throw "signtool.exe not found." }

          & $signtool.FullName remove /s $msix.FullName 2>$null
          & $signtool.FullName sign /fd SHA256 /f $pfx /p $pwdText $msix.FullName
          if ($LASTEXITCODE -ne 0) { throw "Signing failed." }
          & $signtool.FullName verify /pa /v $msix.FullName
          if ($LASTEXITCODE -ne 0) { throw "Signature verification failed." }

          $dest = Join-Path $out "sm64ex-uwp-NTSC-FR_1.0.0.1_x64.msix"
          Copy-Item $msix.FullName $dest -Force
          Remove-Item $pfx -Force

          Get-FileHash $dest -Algorithm SHA256 | Format-List | Out-File (Join-Path $out "SHA256.txt")
          Get-Content "$env:GITHUB_WORKSPACE\sm64ex-src\ports\uwp\Package.appxmanifest" | Out-File (Join-Path $out "Package.appxmanifest.txt")

      - name: Upload installable package
        uses: actions/upload-artifact@v4
        with:
          name: sm64ex-uwp-NTSC-FR-1.0.0.1
          path: deliverable/*
          if-no-files-found: error
